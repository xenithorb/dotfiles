#!/bin/bash

export SSHRC_D="${SSHHOME}/.sshrc.d"
SOURCE_FILES=(
	"${SSHRC_D}/.aliases"
	"${SSHRC_D}/.common"
)

for i in "${SOURCE_FILES[@]}"; do 
	if [[ -f "$i" ]]; then
		. "$i"
	fi
done

export VIMINIT="let \$MYVIMRC='${SSHRC_D}/.vimrc' | source \$MYVIMRC"



