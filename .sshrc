#!/bin/bash

SSHRC_D="${SSHHOME}/.sshrc.d"
SOURCE_FILES=(
	"${SSHRC_D}/.common" #Common should be first here
	"${SSHRC_D}/.aliases"
)

for i in "${SOURCE_FILES[@]}"; do 
	if [[ -f "$i" ]]; then
		. "$i"
	fi
done

